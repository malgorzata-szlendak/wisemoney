import icons from "./icons";
import {CategoryEnum, IconsEnum} from "./enums";
import theme, { COLORS, SIZES, FONTS } from "./theme";

export { icons, theme, COLORS, SIZES, FONTS, CategoryEnum, IconsEnum};